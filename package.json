{
  "name": "slideshowpro",
  "version": "0.1.1",
  "private": true,
  "description": "Thin Tauri Windows wrapper + Android TV WebView shell for SlideShowX",
  "scripts": {
    "sync-ui": "node scripts/sync-ui.mjs",
    "sync-android": "node scripts/sync-android-ui.mjs",
    "tauri": "tauri",
    "dev": "node scripts/sync-ui.mjs && tauri dev",
    "build": "node scripts/sync-ui.mjs && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2"
  }
}
