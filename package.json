{
  "name": "slideshowpro",
  "version": "0.1.0",
  "private": true,
  "description": "Thin Tauri Windows wrapper for SlideShowPro",
  "scripts": {
    "sync-ui": "node scripts/sync-ui.mjs",
    "tauri": "tauri",
    "dev": "node scripts/sync-ui.mjs && tauri dev",
    "build": "node scripts/sync-ui.mjs && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2"
  }
}
